export type PropsLayout = {
  content: JSX.Element;
};

export type StatesLayout = {
  theme: string;
};

export type ActionsLayout = {
  type: 'changeTheme';
};

export type Colors = {
  theme: string;
  background: string;
  inverted: string;
};
