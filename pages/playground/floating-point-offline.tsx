import Controller from 'components/games/floating-point-offline/controller';

const FloatingPointOffline: React.FC = (): JSX.Element => {
  return <Controller />;
};

export default FloatingPointOffline;
