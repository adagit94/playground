import Controller from 'components/games/floating-point-online/controller';

const FloatingPointOnline: React.FC = (): JSX.Element => {
  return <Controller />;
};

export default FloatingPointOnline;
